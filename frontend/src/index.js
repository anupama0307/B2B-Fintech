import React from 'react';
import "./index.css"; // Ensure the path matches
import RiskAnalysisPage from "./pages/admin/RiskAnalysisPage"; // Correct path for the RiskAnalysisPage
import ReactDOM from 'react-dom/client';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React. StrictMode>
    <App />
  </React.StrictMode>
);